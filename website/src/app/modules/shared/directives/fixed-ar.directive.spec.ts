import { FixedArDirective } from "./fixed-ar.directive";

describe("FixedArDirective", () => {
  it("should create an instance", () => {
    const directive = new FixedArDirective();
    expect(directive).toBeTruthy();
  });
});
